---

---

<section class="header-section w-screen h-[55vh] flex flex-col justify-center items-center">

    <h1 class="site-title">VAHIS<span class="outline-letter">H</span>T</h1>

</section>

<style>
    .header-section {
        background-color: #0a1020;
        position: relative;
    }

    /* vignette */
    .header-section::before {
        content: '';
        position: absolute;
        inset: 0;
        background: radial-gradient(ellipse at center, transparent 25%, rgba(4, 8, 20, 0.7) 100%);
        pointer-events: none;
        z-index: 1;
    }

    .site-title {
        font-family: 'Orbitron', sans-serif;
        font-size: clamp(5rem, 15vw, 14rem);
        font-weight: 900;
        letter-spacing: 0.05em;
        color: #ffffff;
        line-height: 1;
        margin: 0;
        user-select: none;
        position: relative;
        z-index: 2;
    }

    .outline-letter {
        color: transparent;
        -webkit-text-stroke: 3px #ffffff;
    }
</style>