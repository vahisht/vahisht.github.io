---
import Layout from "../layout/Layout.astro";
import SpeedrunBands from "../components/speedrun/SpeedrunBands.svelte";
import SeriesSelect from "../components/speedrun/SeriesSelect.svelte"
import DialogBox from "../components/speedrun/DialogBox.svelte"
---

<Layout title="Speedruns">

    <!-- min-height fills the viewport (nav is 64px) so the lowest non-empty band -->
    <!-- stretches via flex:1, continuing its pattern into any empty space below. -->
    <div style="display: flex; flex-direction: column; min-height: calc(100vh - 64px);">

        <!-- Filter bar — full-width sky-blue band, content width-limited -->
        <div style="background: linear-gradient(135deg, #7dd3fc 0%, #38bdf8 100%);">
            <div class="w-full mx-auto px-4 py-5" style="max-width: 1400px;">
                <SeriesSelect client:load />
            </div>
        </div>

        <!-- Reactive bands: runs + commentary, backgrounds controlled by Svelte -->
        <SpeedrunBands client:load />

        <DialogBox client:load />

    </div>

</Layout>